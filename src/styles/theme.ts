import { createTheme } from "@mui/material";

export const theme = createTheme({
  typography: {
    fontFamily: "Roboto",
    fontSize: 12,
  },
  palette: {
    primary: {
      main: "#FF01FB",
    },
  },
  spacing: 4,
});
